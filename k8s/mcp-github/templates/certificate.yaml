apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mcp-github-tls
  namespace: mcp-github
spec:
  secretName: mcp-github-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.hostname }}
