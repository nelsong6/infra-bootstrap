apiVersion: v2
name: arc-controller-wrapper
version: 0.1.0
description: |
  Actions Runner Controller (ARC) — reconciles AutoscalingRunnerSet CRs
  into GitHub Actions runner pods. One controller serves all scale sets
  across all namespaces.
dependencies:
  - name: gha-runner-scale-set-controller
    version: 0.12.1
    repository: oci://ghcr.io/actions/actions-runner-controller-charts
