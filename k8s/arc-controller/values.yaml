# Actions Runner Controller defaults are fine for a single-tenant cluster.
# Just pin log level and resources conservatively.
gha-runner-scale-set-controller:
  flags:
    logLevel: info
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 256Mi
